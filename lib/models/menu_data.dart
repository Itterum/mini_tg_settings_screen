import 'package:flutter/material.dart';

class MenuRowData {
  final IconData icon;
  final String text;
  final Color color;
  final Widget screen;

  MenuRowData(this.icon, this.text, this.color, this.screen);
}
